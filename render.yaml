services:
  # Frontend Web Service
  - type: web
    name: xeno-frontend
    env: node
    region: frankfurt
    plan: free
    buildCommand: cd web && npm install && npm run build
    startCommand: cd web && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
