# Xeno Demo Web

Next.js frontend for the Xeno demo.

Run locally:

```powershell
cd web
npm install
npm run dev
```

Set environment variable `NEXT_PUBLIC_BACKEND_URL` to point at the backend, e.g. `http://localhost:4000`.

The app expects you to paste a tenant `apiKey` (created from the backend) on the landing page.
